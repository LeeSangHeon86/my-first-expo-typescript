export const theme = {
  background: '#111111',
  itemBackground: '#333333',
  main: '#778bdd',
  text: '#eeeeee',
  done: '#666666',
};

export type themeType = {
  theme: {
    background?: string;
    itemBackground?: string;
    main?: string;
    text?: string;
    done?: string;
  };
};
